// Helpers unit tests removed due to deprecation of per-run artifact helpers.
// Covered functionality now exercised via higher-level tests in last-run.test.ts.
describe('deprecated helpers placeholder', () => {
  test('placeholder', () => {
    expect(true).toBe(true);
  });
});
